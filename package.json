{
  "name": "react-simple-context",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.21.1",
    "bootstrap": "^5.1.1",
    "bootstrap-icons": "^1.5.0",
    "concurrently": "^6.3.0",
    "json-server": "^0.16.3",
    "nanoid": "^3.1.23",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3"
  },
  "scripts": {
    "dev:client": "react-scripts start",
    "dev:server": "json-server -w todos.json -p 5000 -d 1000",
    "prod:build": "react-scripts build",
    "start": "concurrently \"yarn dev:server\" \"yarn dev:client\""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}